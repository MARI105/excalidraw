{
  "name": "excalidraw-overlay-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "@excalidraw/excalidraw": "*",
    "browser-fs-access": "0.29.1"
  },
  "devDependencies": {
    "vite": "5.0.12",
    "typescript": "^5"
  },
  "scripts": {
    "start": "vite",
    "build": "yarn run copy:fonts && vite build",
    "preview": "vite preview --port 5002",
    "build:preview": "yarn build && yarn preview",
    "build:package": "yarn workspace @excalidraw/excalidraw run build:esm",
    "copy:fonts": "cp -r ../node_modules/@excalidraw/excalidraw/dist/prod/fonts public/fonts",
    "rm:build": "rm -rf {build,dist}",
    "rm:node_modules": "rm -rf node_modules",
    "rm:fonts": "rm -rf public/fonts"
  }
}
